apiVersion: v1
kind: Service
metadata:
  labels:
    app: bonustriggeragent
    environment: Development
  name: bonustriggeragent
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: bonustriggeragent
  sessionAffinity: None
  type: ClusterIP
